grafana:
  enabled: true
  adminPassword: "homelab123"
  service:
    enabled: true
    type: NodePort
    port: 80
    targetPort: 3000
    nodePort: 31000

prometheus:
  enabled: true
  prometheusSpec:
    retention: 7d

alertmanager:
  enabled: true
  alertmanagerSpec: {}

prometheusOperator:
  admissionWebhooks:
    enabled: false
    patch:
      enabled: false
  tls:
    enabled: false
